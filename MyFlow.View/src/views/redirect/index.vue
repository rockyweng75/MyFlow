<template></template>
<!-- <script>

    import { useRouter, useRoute } from 'vue-router'

    export default{
        setup() {
            const router = useRouter()
            const route = useRoute();
            console.log(route.path)
            router.replace(route.path.replace('/redirect'));
            return{

            }
        },
    }
</script> -->